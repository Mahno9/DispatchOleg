# Платформа DispatchOleg — оболочка терминала

Этот документ описывает несущую конструкцию проекта: layout терминала, экраны и переходы между ними,
расширенный контракт мини-игр, схему БД и логику прогресса/блокировок. Специфика конкретных мини-игр —
в `docs/minigames/`. Формат диалогов — в `docs/dialogue-system.md`. QR — в `docs/qr-flow.md`.

Стиль оформления — везде по `STYLE.md` (палитра, CRT-оверлей, угловатые панели, карточки-досье).
Референс layout — `Windows.png` (4 кадра: МЕТА, окно чтения QR, начало игры/диалог, мини-игра).

---

## 1. Layout терминала

Экран приложения (`web/player`) всегда состоит из двух вертикальных зон:

```
┌─────────────────────────────────────────────────────────┐
│                                                           │
│                РАБОЧАЯ ОБЛАСТЬ (центр)                   │
│        (мета-сцена / QR-сканер / диалог / мини-игра)     │
│                                                           │
├─────────────────────────────────────────────────────────┤
│  [ КАМЕРА ]   [       КОНТЕКСТНЫЙ СЛОТ (центр)     ]  [ КНОПКА ]│
└─────────────────────────────────────────────────────────┘
```

Нижняя панель **постоянна** — она не размонтируется при смене экрана, меняется только содержимое трёх
слотов. Это отдельный React-компонент `BottomBar`, живущий на уровень выше стейт-машины экранов.
На экране мини-игры между слотом 2 и кнопкой появляется четвёртый, необязательный слот — портрет
персонажа игры (см. §1.4).

### 1.1 Слот 1 — камера (слева)

- До завершения обучалки: заглушка «NO SIGNAL» (статичный паттерн шума/CRT-рамка, по STYLE.md — контурная
  иконка камеры + текст на янтарной статусной полосе).
- После обучалки: `<video>` из `CameraPanel`, стрим из singleton-модуля `camera.ts` (см. §4 плана,
  раздел «Камера игрока» — детали в `docs/qr-flow.md`, т.к. стрим совместно используется со сканером).
- Что именно показать вместо кадра, решает `cameraPlate` (`ui/CameraPanel.tsx`): стрим не запрошен —
  «NO SIGNAL» + кнопка «Включить», идёт запрос — «ЗАПРОС» без кнопки, отказ — «NO SIGNAL» +
  «Переподключить», пропавший посреди смены стрим (`SignalLost`) — красное «SIGNAL LOST» + та же
  кнопка. Сам `getUserMedia` панель по своей инициативе не зовёт: в Safari и на iOS запрос без жеста
  игрока — гарантированный отказ. Исключение — уже выданное разрешение (`navigator.permissions`),
  тогда камера поднимается сама после перезагрузки страницы.
- Размер слота фиксирован во всех экранах — камера не меняет размер и позицию, меняется только то, что
  происходит в центральной рабочей области.

### 1.2 Слот 2 — контекстный (центр)

Это единственный слот, чьё содержимое зависит от текущего экрана:

| Экран | Содержимое слота 2 |
|---|---|
| meta | Прогресс по всей игре: `N / M игр пройдено` + горизонтальная полоса-индикатор |
| qr-scan | (по Windows.png третий слот пустой/декоративный — рамка без текста; можно отобразить подсказку «НАВЕДИТЕ КАМЕРУ НА КОД») |
| dialogue | Текстовое окно диалога — реплика говорящего с typewriter-эффектом, см. `docs/dialogue-system.md` |
| minigame | Прогресс внутри мини-игры — рендерится по колбэку `onProgress(text, percent?)` от самой игры (см. §3.1); та же зона отдаётся под сюжетную реплику игры по `onLine(text, onDismiss?)`, пока реплика висит. Реплика рисуется тем же компонентом и той же печатью, что и диалоговая (`dialogue/Line.tsx`), и подписывается именем персонажа игры |

Слот 2 — единственная часть нижней панели, которую могут «арендовать» и мета, и диалог, и мини-игра.
Компонент физически один (`ContextSlot`), контент передаётся через проп в зависимости от активного экрана.

### 1.3 Слот 3 — контекстная кнопка (справа)

| Экран | Содержимое слота 3 |
|---|---|
| meta | Кнопка **START** — открывает qr-scan |
| qr-scan | Кнопка «ОТМЕНА» — возврат в meta |
| dialogue | Пусто (клик для продолжения — по всей области диалога, не по кнопке) либо не рендерится вовсе |
| minigame | Кнопка «Выйти». Мини-игра **не может** писать в этот слот (изоляция контракта — см. §3): кнопкой владеет платформа. Клик не уничтожает игру сразу, а поднимает подтверждение и замораживает игру — см. §2.5 |

---

### 1.4 Слот портрета (экран мини-игры)

На экране `minigame` между слотом 2 и кнопкой встаёт портрет персонажа игры (`games.character_id`) —
тот же кадр связи, что в диалоге: персонаж ведёт игрока по мини-игре и диктует подсказки, и его надо
видеть.

Портрет привязан к голосу, а не к экрану: он появляется, пока в слоте 2 висит реплика игры
(`onLine`), и уходит вместе с ней. Игра, которая ничего не говорит (кухня, тетрис), портрета
в панели не получает — озвучивать нечего. Нет персонажа у игры — тоже нет. Без портрета панель
остаётся трёхслотовой и раскладка не съезжает (`.bottombar-portrait` меняет сетку целиком).

Кнопка остаётся на своём месте — портрет встаёт слева от неё, а не вместо. Пока в двухголосой
реплике говорит игрок, портрет персонажа гаснет (`.portrait-muted`) — ровно как слушающая сторона
в диалоговой сцене.

## 2. Экраны и переходы

Роутинг — состоянием (finite state), без клиентского роутера (в SPA терминала нет multi-page навигации,
URL не меняется). Состояние экрана хранится в верхнем компоненте `Terminal` (или сторе state-machine типа
`useReducer`/xstate-подобного редьюсера — конкретная реализация на усмотрение фазы кодирования).

Экранов семь (`type Screen` в `web/player/src/App.tsx`): `onboarding`, `meta`, `qr-scan`, `launch`,
`dialogue`, `minigame`, `victory`.

```
        ┌────────────┐
        │ onboarding │  (один раз, до onboarded=true)
        └─────┬──────┘
              │ успешный скан QR обучалки
              ▼
   ┌────────────────────┐        START           ┌──────────┐
   │        meta         │ ─────────────────────▶ │ qr-scan  │   (при NO_QR=1 экрана нет:
   │ (персонажи, прогресс)│ ◀───────────────────── │          │    START берёт игру жребием)
   └─────────┬────────────┘   отмена / ошибка скана└─────┬────┘
             ▲                                            │ валидный QR открытой игры
             │                                            ▼
             │                                    ┌────────────────┐
             │                                    │     launch      │  «Загрузка задания…» —
             │                                    └───────┬────────┘   пока едет конфиг игры
             │                                            ▼
             │                                    ┌────────────────┐
             │                                    │ dialogue (pre)  │  (если у игры задан pre-диалог;
             │                                    └───────┬────────┘   иначе шаг пропускается)
             │                                            ▼
             │                                    ┌────────────────┐
             │                                    │    minigame     │
             │                                    └───────┬────────┘
             │                                            │ onComplete({score, won, details?})
             │                                            ▼
             │                                    ┌────────────────┐
             │                                    │ dialogue (post) │  (post-win / post-lose /
             │                                    └───────┬────────┘   styleTag-ветка, см. §3.2)
             │                                            │
             └────────────────────────────────────────────┘
                        возврат в meta (обновлённый прогресс)

   meta ──▶ victory   (все не-обучающие игры выиграны; один раз за прохождение)
```

Также кликабельны персонажи меты — клик открывает их `meta_dialogue_id` без перехода в qr-scan/minigame
(диалог → meta напрямую, ветка «болталка», не влияет на прогресс).

### 2.1 onboarding

Платформенный сценарий, не мини-игра из общего бандла (см. `docs/minigames/01-onboarding.md`). Идёт первым
запуском приложения, пока `users.onboarded = false`. Итог — `onboarded = true` в sync-состоянии, включённая
камера, переход в meta.

### 2.2 meta

Мета-сцена (`screens/MetaScreen.tsx`) — это **персонажи на фоне и больше ничего**: фон и расстановка
берутся из текущего этапа `meta_stages` (§4), а если ни один этап не сработал — из запасной раскладки
по `characters.meta_position` (две колонки, слева/справа). Клик по персонажу открывает его диалог;
уже прочитанные помечаются, непрочитанные гейтят кнопку START (см. ниже).

Карточек игр на мете нет. Прогресс живёт в слоте 2 нижней панели: строка
`операций завершено N / M · доступно K` и сегментная полоса по числу не-обучающих игр. Пока в текущем
этапе остались непрочитанные диалоги, START заблокирован и в слоте 2 висит
`Сначала опросите персонал · осталось N`; как только осталось ноль — кнопка начинает мигать.

> Не реализовано: карточки-досье игр с силуэтом и иконкой замка для закрытых (`required_game_ids` не все
> `won`). Замысел сохраняем — но сегодня закрытая игра игроку на мете вообще не показана, а отказ он
> получает только на скане, от сервера (§5).

### 2.3 qr-scan

Общий сканер (`camera/QrScanner.ts` через `ui/ScanView`, см. `docs/qr-flow.md`) в рабочей области.
Успешный декод → `POST /api/qr/verify` → при успехе переходим на `launch`. Разбор ответа —
`verifyVerdict` в `screens/QrScanScreen.tsx`:

- **`ok`** — игра уходит в цепочку запуска;
- **`ok`, но `game.isTutorial`** — коробочный код обучалки отбивается вспышкой `ОБУЧЕНИЕ УЖЕ ПРОЙДЕНО`:
  у системной игры нет бандла, и запуск обернулся бы «Сбоем запуска». Скан продолжается;
- **`locked`** — панель «Доступ запрещён» с перечислением недостающих игр по названию
  (`requiredTitles`); сканер на это время встаёт, кнопка «ОК» возвращает в meta;
- **прочие отказы и не-`dispatch:` коды** — короткая вспышка, сканер продолжает работать.

Отдельно от отказов сервера — отказ камеры: если `getUserMedia` не дал стрим или стрим пропал посреди
скана (`SignalLost`), поверх сканера встаёт панель с причиной и кнопкой **«ПЕРЕПОДКЛЮЧИТЬ»**, которая
просит стрим заново. Пока панель висит, кадры не разбираются.

### 2.3a launch

Промежуточный экран между сканом и цепочкой: `startGame` уходит за конфигом игры
(`GET /api/games/:id/config`), а игрок видит панель «Загрузка задания…», название операции в слоте 2 и
кнопку «Отмена». Конфиг доехал — идём в `dialogue(pre)` или сразу в `minigame`; не доехал — возврат
в meta с русской строкой ошибки в слоте 2 (номер запуска в `runRef` отсекает ответ отменённого).

### 2.4 dialogue

Рендерер `DialogueScene` (детали — `docs/dialogue-system.md`) в рабочей области, реплики зеркалятся в
текстовое окно слота 2 нижней панели. Три точки использования диалога в этом переходе:
- **pre** — перед мини-игрой (`games.pre_dialogue_id`);
- **post-win** — после победы (`games.post_win_dialogue_id`), может быть переопределён styleTag-веткой;
- **post-lose** — после поражения (`games.post_lose_dialogue_id`).

Диалог без узлов (не задан в админке) — экран пропускается автоматически, без «пустого» показа.

### 2.5 minigame

Рабочая область целиком передаётся под `container` мини-игры (см. §3). Нижняя панель остаётся
платформенной: слот 1 (камера) не трогается игрой, слот 2 обновляется через `onProgress`, слот 3
управляется платформой, а не игрой.

Кнопка «Выйти» в слоте 3 **не уничтожает игру сразу**: она поднимает подтверждение
(`ui/ExitConfirm.tsx` — «Прервать операцию?», кнопки «ПРЕРВАТЬ» / «ПРОДОЛЖИТЬ»), а игра на это время
замирает — платформа передаёт `paused` в `MinigameScreen`, тот зовёт `handle.setPaused(true)`
(`minigame_contract.md`, «Пауза»). Отмена возвращает игру в тот же кадр, подтверждение уводит в meta
без записи попытки. Штатное завершение игры уводит с экрана само, подтверждения не спрашивает.

### 2.6 victory

Экран финала (`screens/VictoryScreen.tsx`) с текстом из настройки `final_victory_text`. Срабатывает
один раз за прохождение, когда выиграны **все** не-обучающие игры, и только на мете: диалог и
запущенная мини-игра дорабатывают до конца и возвращаются на мету, где экран и всплывает. Факт показа
хранится в `localStorage` (`dispatch_victory_seen`); неполная зачистка — сброс админкой, добавленная
игра — снова взводит его. В тест-режиме (`?test=…`) флаг не трогается.

---

## 3. Расширенный контракт мини-игр

База — `minigame_contract.md` (GameService): `init(container, config, callbacks)`, обязательный `destroy()`,
изоляция (никаких `localStorage`/`window.*`/глобальных стилей), звук через `config.muted`, 60 FPS,
`schema.json` для конфигурации. Всё это остаётся без изменений. Платформа DispatchOleg добавляет три
расширения.

### 3.1 `onProgress(text, percent?)`

Необязательный колбэк в объекте `callbacks`, наравне с `onComplete`/`onExit`:

```ts
callbacks.onProgress(text: string, percent?: number): void
```

- Игра может вызывать его произвольное число раз за время своей работы (в отличие от `onComplete`/`onExit`,
  которые вызываются ровно один раз и завершают жизненный цикл).
- Платформа рендерит `text` и, если передан, `percent` (полоса-индикатор 0–100) в слоте 2 нижней панели —
  это ровно раздел «Прогресс внутри мини-игры» на Windows.png.
- Колбэк — не обязателен к вызову; игра, которой нечего показать (например, `tetris-fill` может просто
  слать «Собрано: N/M деталей»), не обязана вызывать его каждый кадр — платформа не сбрасывает текст
  автоматически, последний переданный `text` остаётся до следующего вызова или до конца игры.
- Вызов `onProgress` **не входит** в правило «ровно один из колбэков» — это самостоятельный, многократный,
  не завершающий канал.

Тот же слот 2 умеет показывать сюжетную реплику игры поверх прогресса:

```ts
callbacks.onLine?.(text: string | null, onDismiss?: () => void): void
```

- Пока передан `text`, слот 2 рисует реплику вместо строки `onProgress`; `null` возвращает её обратно.
- Клик игрока по слоту 2 форвардится в `onDismiss` — момент и решение «пора убрать» целиком за игрой,
  платформа слот сама не чистит.
- Как и `onProgress` — вызывать по смене текста, не каждый кадр; колбэк опционален (`?.`).
- Реплика рисуется тем же компонентом и той же печатью, что и диалоговая (`dialogue/Line.tsx`),
  и подписывается именем персонажа игры. Префикс `«ИМЯ: текст»` с известным именем (персонаж игры
  или игрок) платформа вынимает в подпись и по нему же выбирает сторону — см. `minigame_contract.md`.

### 3.2 `onComplete({score, won, details?})`

Результат расширяется необязательным полем `details`:

```ts
{
  score: number,
  won: boolean,
  details?: Record<string, number | string>
}
```

- `details` — произвольная статистика конкретной игры, не влияющая на контракт напрямую. Пример:
  `three-mazes` возвращает `{ wallsBroken: 2, totalBreakable: 5, styleTag: "breaker" }`.
- `details` сохраняется целиком в `gameResults[gameId].details` через offline-sync (см. §6) и доступен
  платформе после завершения игры — в первую очередь для ветвления пост-диалога (см. ниже) и для админки
  (вкладка «Игроки» может показать `details` как есть, для отладки/аналитики).
- Игры, которым нечего возвращать в `details`, просто не указывают это поле — обратная совместимость с
  базовым контрактом полная.

### 3.3 Ветвление пост-диалога по `styleTag`

> Механика в коде есть (`pickPostDialogue` в `dialogue/engine.ts`, колонка `style_dialogues_json`,
> поле в форме игры), **но контент под неё не заведён**: во всех играх `style_dialogues_json` пустой,
> тексты стилевых веток на ревью. Пока карта пуста, ветвление ни на что не влияет — работает обычный
> `post_win_dialogue_id`.

У игры (таблица `games`), помимо `post_win_dialogue_id`, есть карта
`style_dialogues_json: Record<styleTag, dialogueId>` — отдельная колонка, в конфиг мини-игры она не
входит (плееру приезжает как `styleDialogues` в `GET /api/games/:id/config`).

Логика выбора пост-диалога при `won = true`:
1. Если `details.styleTag` присутствует **и** для него в `style_dialogues_json` есть запись → показывается
   диалог из этой записи.
2. Иначе → показывается `post_win_dialogue_id` как обычно.
3. Если `won = false` → всегда `post_lose_dialogue_id`, `styleTag` не рассматривается (ветвление стиля
   имеет смысл только для победных исходов — «как именно ты выиграл», а не «как проиграл»).

Так результат «какого персонажа отыгрывал игрок» (например, «призрак» vs «крушитель» в `three-mazes`)
влияет на сюжетную реакцию мета-персонажей, не меняя саму механику разблокировки прогресса.

### 3.4 Плейсхолдер `{player}` в конфиге

Перед `init` лоадер проходит по собранному конфигу (дефолты мини-игры ⊕ переопределения игры) и заменяет
во **всех строках, на любой глубине**, подстроку `{player}` на имя игрока из профиля (`ГОСТЬ` без имени —
подставляется `Диспетчер`). Мини-игра плейсхолдера не видит и ничего про него не знает: ей приходит готовое имя.

- В админке в любое текстовое поле конфига пишется `{player}` — например `playerName: "{player}"` и
  `tasks[].assignee: "{player}"` в `task-sort`, чтобы «свои» задачи были своими для любого игрока.
- Замена рекурсивная и по всем вхождениям (`replaceAll`), потому что подставлять надо не только в
  верхнеуровневые поля, но и внутрь массивов объектов.
- Совпадение имени игрока с именем NPC в конфиге (игрок назвался «Стас», а `Стас` — чужой исполнитель)
  ничем не разруливается: чужая задача станет своей. Отдельного экранирования не заводили.

### 3.5 Экран загрузки, ассеты и инструктаж

Пока `launchMinigame` не отработал, поверх рабочей области висит непрозрачный экран «Загрузка…»
(`.minigame-loading`). Под ним параллельно грузятся бандл игры и все её ассеты: каждая строка конфига,
начинающаяся с `/assets-store/`, скачивается целиком в память, и игре в конфиг уходит уже `blob:`-адрес
(`preloadAssets` в `minigameLoader.ts`). Игры создают `new Audio(url)` в момент `play()` — без этого
первый звук каждого вида ждал сети. Что не скачалось, остаётся оригинальным адресом. На `destroy`
blob-адреса отзываются, память под ассеты освобождается; сам ES-модуль бандла браузер держит в кэше
модулей — его выгрузить нельзя.

Инструктаж показывается только после загрузки. Шаги — в `web/player/src/game/tutorials.ts`, ключ
`minigameId`. Игра под инструктажем уже смонтирована, но заморожена (`setPaused`), поэтому оверлей
измеряет её живой DOM, а не хранит выверенные координаты.

**Показывается он один раз на мини-игру.** Закрытый по «Понятно» инструктаж кладёт `minigameId` в
`briefedMinigames` состояния игрока (`state/localState.ts`, поле едет в `/api/sync` и объединяется
как `seenDialogues`), и на повторной попытке стрелки сами не всплывают — иначе игрок кликал бы их
перед каждым заходом. Вызвать их обратно можно кнопкой **«?»** в углу рабочей области
(`.tut-again`): она есть, пока у игры вообще заведены шаги, и снова замораживает игру на время
показа. Игра без шагов стартует сразу и кнопки не получает.

Про раскладку самих стрелок:

- Шаг — это `{ target: 'CSS-селектор', text }`. Стрелка садится на найденный элемент, направление и точку
  внутри цели считает `resolveStep`: цель в левой трети экрана — стрелка смотрит влево и подпись уходит
  вправо, в правой — зеркально, посередине — вверх/вниз. Подпись всегда уезжает к центру.
- Проценты рабочей области были бы враньём: игры вписывают поле с полями по краям, и одна и та же точка
  на мониторе и на телефоне попадает в разные места. Было ровно так — три стрелки `task-sort` на широком
  экране втыкались в одну колонку.
- `aspect` — для игр целиком на `<canvas>` (`rescue-catch`, `three-mazes`): холст растянут на всю область,
  а мир рисуется по центру, и без пропорции стрелка целится в холст, а не в картинку. Там же оставлены
  ручные `x`/`y`: это точки мира, отдельных элементов под них нет.
- Подписи после размещения расталкиваются по вертикали и зажимаются внутрь области — двигается **текст**,
  стрелка остаётся на цели. Поэтому новый шаг обычно не требует подгонки координат вообще.
- Раскладка проверена скриптом Playwright на шести играх в пяти пропорциях: подпись не выходит за край и
  не наезжает на соседнюю. Единственное известное касание — `safe-crack` на узком экране, 10 px рамок.

### 3.6 Что не меняется

- Мини-игра не имеет доступа к слоту 1 и слоту 3 нижней панели — они не часть `container`.
- `container`, передаваемый мини-игре — это рабочая область экрана `minigame`, без нижней панели.
  Технически это собственный `position:absolute; inset:0` узел запуска внутри неё, а не сама рабочая
  область: под StrictMode эффект монтируется дважды, и отменённый запуск, дорезолвившись после живого,
  вычищал общий контейнер — экран гас через раз. Свой узел каждый запуск сносит только за собой.
- Изоляция (destroy, отсутствие глобальных side-effect'ов), звук (`config.muted`), тестовый изолированный
  запуск из админки — всё как в базовом контракте, `onProgress` в изолированном режиме — no-op с логом в
  консоль (аналогично заглушкам `onComplete`/`onExit` в тестовом режиме).

---

## 4. Схема БД

SQLite, миграции последовательные (`server/src/db/migrate.ts`, файлы —
`server/src/db/migrations/*.sql`). Ниже — фактические таблицы; источник истины по типам и
ограничениям всегда сам SQL миграции, а не эта сводка.

### `users`
| Поле | Тип | Описание |
|---|---|---|
| `id` | TEXT PK | nanoid, выдаётся при регистрации; он же уходит в `POST /api/qr/verify` |
| `name` | TEXT NOT NULL UNIQUE COLLATE NOCASE | Регистрация по имени, без пароля |
| `onboarded` | INTEGER (0/1) | Флаг завершения обучалки |
| `created_at` | INTEGER (ms) | |

### `game_states`
| Поле | Тип | Описание |
|---|---|---|
| `user_id` | TEXT PK, FK → users.id ON DELETE CASCADE | |
| `payload` | TEXT (JSON) | `{ gameResults: { [gameId]: { won, bestScore, attempts, details? } }, onboarded, seenDialogues, briefedMinigames }` |
| `client_updated_at` | INTEGER (ms) | Метка клиента — для client-authoritative merge |
| `synced_at` | INTEGER (ms) | Метка сервера — последняя синхронизация |

### `games`
| Поле | Тип | Описание |
|---|---|---|
| `id` | INTEGER PK AUTOINCREMENT | Числовой; он же попадает в QR-payload (`dispatch:<id>:<hmac>`) |
| `title` | TEXT NOT NULL | Название для админки/меты |
| `minigame_id` | TEXT NOT NULL | Ссылка на директорию в `/minigames/` (`rescue-catch`, `task-sort`, …) |
| `config_json` | TEXT (JSON) | Конфиг по `schema.json` игры |
| `character_id` | INTEGER, FK → characters.id ON DELETE SET NULL | Персонаж, связанный с игрой (портрет и голос в мини-игре) |
| `pre_dialogue_id` | INTEGER, FK → dialogues.id ON DELETE SET NULL | |
| `post_win_dialogue_id` | INTEGER, FK → dialogues.id ON DELETE SET NULL | |
| `post_lose_dialogue_id` | INTEGER, FK → dialogues.id ON DELETE SET NULL | |
| `style_dialogues_json` | TEXT (JSON) | Карта `styleTag → dialogueId` (см. §3.3), отдельной колонкой, а не внутри `config_json` |
| `required_game_ids_json` | TEXT (JSON, массив id) | Условие разблокировки — все перечисленные должны быть `won` |
| `sort_order` | INTEGER | Порядок в списке меты |
| `is_tutorial` | INTEGER (0/1) | `1` для обучалки — не показывается в обычном списке игр меты |

Отдельного `slug` у игры нет, и **пер-игрового секрета QR тоже нет**: подпись считается одним
серверным ключом `QR_SECRET` (см. `docs/qr-flow.md` §1).

### `characters`
| Поле | Тип | Описание |
|---|---|---|
| `id` | INTEGER PK AUTOINCREMENT | |
| `name` | TEXT NOT NULL | |
| `portrait_asset` | TEXT (URL) | |
| `meta_dialogue_id` | INTEGER, FK → dialogues.id ON DELETE SET NULL | Диалог по клику на мете |
| `meta_position` | TEXT, по умолчанию `'left'` | Сторона на запасной мета-сцене; расстановку по координатам задаёт `meta_stages` |
| `description` | TEXT, по умолчанию `''` | Сводка для игрока: всплывает по наведению на портрет (в диалоге и на мете). Пустая — подсказки нет |

### `dialogues`
| Поле | Тип | Описание |
|---|---|---|
| `id` | INTEGER PK AUTOINCREMENT | |
| `title` | TEXT NOT NULL | Для списка в админке |
| `nodes_json` | TEXT (JSON) | Формат узлов — см. `docs/dialogue-system.md` |

### `meta_stages`
Этапы мета-сцены (миграция `0002_meta.sql`): фон и расставленный вручную каст, который сменяется
по мере прохождения.

| Поле | Тип | Описание |
|---|---|---|
| `id` | INTEGER PK AUTOINCREMENT | |
| `title` | TEXT, по умолчанию `''` | Заголовок сцены в шапке меты |
| `sort_order` | INTEGER | Порядок разбора этапов |
| `background_json` | TEXT (JSON) | Фон сцены: `{ image?, fit?, scale?, offset? }` |
| `characters_json` | TEXT (JSON) | Расстановка: `[{ characterId, x, y, scale?, dialogueId? }]`, x/y — проценты сцены |
| `trigger_json` | TEXT (JSON) | `{"type":"wonCount","value":N}` либо `{"type":"games","ids":[…]}`; по умолчанию `{"type":"wonCount","value":0}` |

Текущая — **последняя** по `(sort_order, id)`, чьё условие выполнено; ни одна не сработала — мета
рисует запасную раскладку по `characters.meta_position` (`screens/metaStage.ts`).

### `minigame_defaults`
`minigame_id` PK + `config_json` + `updated_at`: дефолтный конфиг типа игры, поверх которого
кладутся переопределения конкретной игры.

### `assets`, `settings`
`assets` — метаданные загруженных файлов (`id`, `kind` ∈ `image|audio|gif`, `mime`, `ext`,
`original_name`, `size_bytes`, `created_at`); отдача и загрузка — `server/src/routes/assets.ts`,
см. §7.1. `settings` — key-value для глобальных настроек.

Ключи (`SETTING_KEYS` в `server/src/repos/settings.ts` — источник истины; чужой ключ
`PUT /api/admin/settings` отвергает, у каждого ключа свой валидатор):

| Ключ | Значение | Смысл |
|---|---|---|
| `ui_click_sound_url` | url, `[{url, weight}]` или `null` | Звук нажатия любой кнопки терминала |
| `meta_music_url` | строка или `null` | Фоновая петля лобби — мета, скан, экран запуска; `null` — тишина |
| `sync_interval_s` | целое 5…86400 | Период фонового синка плеера |
| `final_victory_text` | строка или `null` | Текст экрана победы |
| `character_voices` | объект `{ [speakerId]: preset }` или `null` | Пресеты бубнежа персонажей |

Плеер играет `ui_click_sound_url` на нажатие любой `<button>` на всех экранах,
громкостью ползунка эффектов (`sfxVolume`); значение — один файл или взвешенный
список вариантов.

Плеер играет `ui_click_sound_url` на нажатие любой `<button>` на всех экранах,
громкостью ползунка эффектов (`sfxVolume`); значение — один файл или взвешенный
список вариантов.


---

## 5. Блокировки

- У каждой игры — `required_game_ids: string[]`. Игра доступна для скана, если для **каждого** id из списка
  `gameResults[id].won === true` в текущем прогрессе пользователя.
- Проверка выполняется на сервере при `POST /api/qr/verify` (клиент не может обойти проверку, подделав
  локальный стейт) — детали протокола в `docs/qr-flow.md`.
- Отражение в мете: закрытая игра рисуется силуэтом с иконкой замка (контурная, по STYLE.md), без доступа к
  QR-подсказке; сам скан её QR-кода (если физически показать код раньше времени) всё равно получит отказ от
  сервера — фронтовое затемнение не единственная защита.
- Пустой `required_game_ids` (`[]`) — игра открыта всегда (обычно так у первых игр цепочки).

---

## 6. Прогресс и синхронизация

- Клиент-авторитетный sync — копия механизма GameService (`resolveSync` в `server/src/repos/sync.ts`,
  `state/localState.ts` + `state/sync.ts` на клиенте): локальный стейт — источник истины на устройстве,
  сервер мержит по `client_updated_at`, тумбстоуны позволяют админке форс-сбросить прогресс игрока
  (аналог `UserProgressModal` в GameService), не боясь, что клиент «оживит» сброшенное значение
  устаревшим локальным кэшем.
- Единица прогресса — `gameResults[gameId] = { won, bestScore, attempts, details? }`. `attempts`
  инкрементируется при каждом `onComplete` (успешном или нет), `bestScore` — максимум по всем попыткам,
  `won` — булев OR по всем попыткам (однажды пройдено — навсегда пройдено, не сбрасывается повторным
  проигрышем).
- Мета-прогресс-бар — `count(won=true среди игр, is_tutorial=false) / count(всего игр, is_tutorial=false)`.
- «Прогресс в других играх» как условие разблокировки — только `won`, не `bestScore` и не `attempts`
  (допущение из плана, зафиксированное с пользователем).

---

## 7. Админка — вкладки (обзор)

Подробности форм — в соответствующих доках (`docs/dialogue-system.md` для вкладки «Диалоги»,
`docs/qr-flow.md` для генерации QR на вкладке «Игры»). Здесь — только состав:

1. **Игры** — CRUD игры: выбор мини-игры (тип), конфиг через SchemaForm по `schema.json` выбранной игры,
   персонаж, 3 диалога (pre/post-win/post-lose) + карта стилевых веток (`style_dialogues_json`), требования разблокировки
   (мультиселект существующих игр), кнопка генерации/печати QR.
2. **Персонажи** — CRUD: имя, портрет (asset), мета-диалог, позиция на мета-экране.
3. **Диалоги** — редактор узлов + импорт/экспорт JSON (см. `docs/dialogue-system.md`).
4. **Игроки** — список пользователей, их прогресс (`gameResults` + `details`), сброс/форс-завершение
   отдельной игры или всего прогресса.
5. **Ассеты** — загрузка/список/удаление файлов (`server/src/routes/assets.ts`), см. §7.1.
6. **Настройки** — глобальные звуки интерфейса и прочие KV-настройки платформы.
7. **Тест-запуск мини-игры** — изолированный прогон любой игры с текущим конфигом, без прохождения меты
   (аналог `MinigamesSection` в GameService); также используется как превью при отладке `schema.json`.

### 7.1 Ассеты: приём, отдача, удаление

Загрузка — `POST /api/admin/assets` (multipart, только под админ-сессией). Лимиты стоят в
`server/src/app.ts`: **10 МБ на файл, до 20 файлов за запрос**. Потолок низкий намеренно — каждая
часть читается в память целиком (`part.toBuffer()`), а контейнеру выдано 400 МБ; самый тяжёлый
реальный ассет — музыкальная петля на ~1,9 МБ.

Что проверяется на приёме (`server/src/routes/assets.ts`):

- тип должен быть в `MIME_MAP` (png/jpeg/webp/bmp/ico/gif/svg, mp3/ogg/wav/webm/m4a/flac) — иначе файл
  попадает в `rejected` ответа;
- **содержимое сверяется с заявленным типом по сигнатуре** (`contentMatchesMime`): mimetype приходит
  со слов клиента, и без этой проверки под видом `image/png` сохранялось и отдавалось что угодно.
  У SVG сигнатуры нет — там смотрится, похоже ли начало файла на XML с `<svg`;
- SVG дополнительно прогоняется через `svgLooksDangerous` (скрипты, `on*=`, `javascript:`,
  `<foreignObject>`) и при срабатывании отвергается целиком, а не «чистится»;
- FLAC перекодируется в OGG через `ffmpeg`; нет ffmpeg — файл отвергается.

Отдача — `/assets-store/<id>.<ext>` (`@fastify/static`, кэш 30 дней). На каждый ответ вешаются
заголовки `ASSET_HEADERS`: `Content-Security-Policy: default-src 'none'; style-src 'unsafe-inline';
sandbox` и `X-Content-Type-Options: nosniff`. Это и есть настоящая защита от загруженного SVG:
он живёт на том же origin, что админка, по прямой ссылке открывается как документ, и без `sandbox`
его скрипт дотянулся бы до admin-cookie. Регулярка `svgLooksDangerous` — только первый рубеж, она не
видит XML-энтити.

Удаление — `DELETE /api/admin/assets/:id`. Перед ним админка спрашивает
`GET /api/admin/assets/:id/usage`: сервер обходит игры, персонажей, узлы диалогов, мета-этапы и
настройки и возвращает список мест, где встречается url ассета, с точным путём внутри JSON
(`конфиг: sounds.move[0].url`). Список подклеивается к вопросу «Удалить файл?» — ссылка на удалённый
ассет в конфиге не обнуляется, а остаётся битой, поэтому удалять вслепую нельзя.
